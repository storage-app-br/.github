name: Storage Build and Push

on:
  pull_request:
    branches: [$default-branch]
  workflow_dispatch:

jobs:

  CI:
    uses: storage-app-br/pipelines/.github/workflows/storage-build.yml@main
